const ingredients = ["eggs", "milk", "flour", "sugar", "baking soda", "baking powder", "chocolate chips", "bananas"];
console.log('Cake ingredients are:');
for (let i = 0; i < ingredients.length; i++){
    console.log(ingredients[i]);
}